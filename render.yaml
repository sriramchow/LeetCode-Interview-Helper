services:
  - type: web
    name: leetcode-ir-helper
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r backend/requirements.txt && cd frontend && npm ci && npm run build"
    startCommand: "gunicorn --chdir backend app:app --bind 0.0.0.0:$PORT --timeout 120"
